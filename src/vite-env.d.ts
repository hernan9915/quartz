/// <reference types="vite/client" />

// Defined by Vite at build time from package.json. Use this anywhere
// the UI needs to display the current app version — keeps the title
// bar in sync with the actual release without manual edits.
declare const __APP_VERSION__: string;
